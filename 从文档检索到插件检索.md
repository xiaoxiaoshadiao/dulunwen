# 从文档检索到插件检索：Agent能力演进主线
## 一、核心判断
传统检索解决的是“模型需要看到什么信息”。Agent开始执行任务后，检索对象逐渐从信息变成能力：先是可复用的工作流程，再是可调用的工具，最后是能够改变Agent运行时结构的插件。我们目前要讨论的不是插件市场搜索，而是一个更具体的问题：系统是否需要根据用户任务、当前环境和已有Harness状态，动态决定哪些插件应被安装、激活、组合或替换。
这个问题目前只能视为待验证假设。依赖图检索、多工具组合、动态工具调用、MCP执行评测和Harness对比都已有相关工作。要证明插件检索值得成为独立问题，至少需要说明：环境变化确实会改变正确插件方案；平面的逐项排序不足以生成可执行组合；真实插件契约可以被抽取；任务完成后插件的状态和资源可以被检查及回收。
## 二、检索对象如何变化
| 阶段 | 检索对象 | 主要问题 | 选择错误的直接后果 |
|---|---|---|---|
| 文档检索 | 事实、证据、参考文本 | 模型需要知道什么 | 噪声、冲突证据、回答错误 |
| Skill检索 | 程序性知识、步骤、脚本和资源 | 模型应该采用什么流程 | 流程错误、能力缺失或强行组合 |
| Tool检索 | 已经安装并暴露的原子接口 | 模型下一步调用什么 | 调错接口、参数或执行顺序 |
| Plugin检索 | 可安装、可配置、具有依赖和生命周期的运行时组件 | 当前Harness应该具备什么能力 | 依赖冲突、越权、资源浪费、状态污染或无法安全退出 |
这条线不是严格的历史顺序，也不是说后一层会淘汰前一层。插件可以提供Tool，也可以携带Skill；Skill仍适合表达程序性知识，Tool仍是实际执行的原子接口。变化在于系统的最高组合单元逐渐上移：当Harness本身插件化后，仅检索Skill或Tool已经不能完整描述运行时能力如何进入系统。
## 三、我们为什么从Skill检索走到这里
在传统文档检索中，相关文档通常可以分别判断。Skill检索不同，复杂任务可能需要多个Skill协作；每个Skill单独相关，不代表它们在当前需求下能够自然组合。我们此前在R3-Skill中把这个问题拆成单体相关性与查询条件下的兼容性，并保留生成流程中原本会被丢弃的拒绝样本，用于训练和评测多Skill路由。
这个工作仍然主要发生在模型上下文层：检索器决定加载哪些程序性说明。Tool Retrieval进一步接近执行层，研究如何从大量接口中选择工具、补齐依赖并规划调用顺序。但多数工作默认工具所属的服务已经安装、连接并暴露给Agent。Tool Retrieval回答的是“现有能力中调用什么”，没有回答这些能力最初为什么存在于当前运行时。
## 四、插件检索增加了什么
Plugin不是更长的Tool描述。一个插件可能同时注册工具、提供服务、加载Skill、启动进程、管理记忆、读取凭据、改变权限策略或替换模型适配器。选择插件意味着修改Harness组件图，因此判断依据不能只有用户查询，还要包括网络、凭据、文件权限、操作系统、资源预算、当前Provider以及已经激活的插件。
例如用户要求检索论文、下载PDF、建立索引并生成报告。在允许外网且具有API密钥的环境中，系统可能选择远程学术搜索和远程Embedding插件；在禁止外网但存在本地论文镜像和GPU的环境中，同一任务应切换到本地检索和本地模型；在只读文件系统中，下载和建立索引本身可能不可执行，系统应选择只读方案或明确拒绝。这里的输出不再是固定Top-K，而更像一个配置增量：增加或移除哪些插件、依赖绑定到哪个Provider、采用什么配置和权限，以及任务结束后如何退出。
## 五、需要验证的研究问题
第一，查询和环境是否共同决定插件方案。如果去掉环境信息仍能得到同样结果，那么“环境条件插件检索”的必要性就很弱。第二，是否真的需要集合或图级模型。依赖补全、版本约束和Provider选择可能由确定性规则解决，学习模型只需负责语义召回。第三，公开插件是否具有足够稳定的描述、配置、依赖、权限和生命周期信息；如果契约无法抽取，形式化就难以落地。第四，插件组合能否被可靠评测。不能只看召回率，还要观察配置是否可行、任务是否完成、是否越权，以及卸载后是否留下进程、连接、注册项或临时状态。
这些问题应当允许否定答案。早期实验的目标不是证明方案正确，而是判断问题是否真实、边界应当落在哪里，以及学习方法相对规则系统是否有必要。
## 六、研究与业务发展的共同线索
研究侧可以沿着“语义召回—组合验证—执行反馈”展开：先从大目录召回可能相关的根插件，再用依赖和策略约束生成可行配置，最后在隔离环境中执行并收集成功、拒绝、失败和回滚信号。业务侧对应的是能力按需加载：减少默认暴露的工具和上下文，复用已有Provider，控制冷启动、权限和成本，并为插件安装及运行建立统一审计记录。
下一步应先做小规模可执行验证，而不是直接训练复杂模型。可以从公开MCP服务器中选择无需外部凭据的子集，对同一任务构造断网、无凭据、只读权限、Provider已存在等环境变化，比较查询检索、依赖规则和状态条件组合三类方案。只有当环境变化稳定地改变正确答案，且现有方法无法可靠处理时，才有必要继续扩大数据和模型规模。
