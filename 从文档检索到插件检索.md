# 从文档检索到插件检索

> **核心问题：随着Agent从“回答问题”走向“执行任务”，检索对象正在从信息变成能力。**

## 当前进展

**当前阶段：问题主线和相关工作已经梳理完成，尚未进入大规模数据构建和模型训练。下一步是验证“环境是否真的会改变正确插件方案”。**

| 工作 | 状态 | 说明 |
|---|---|---|
| 研究主线 | **已完成** | 已串联文档检索、Skill检索、Tool检索和Plugin检索 |
| 相关工作 | **已完成** | 已归档17篇论文，并完成除R3-Skill外16篇论文的逐篇解读 |
| 数据资源 | **已整理** | 已记录公开数据、代码、Registry和Marketplace入口，数据本体尚未下载 |
| DeepSeek Harness源码 | **待开始** | 后续单独建立技术文档，不放入本主文档 |
| Cordis数学与定义 | **待开始** | 后续与源码分析一起整理为独立技术文档 |
| 可执行问题验证 | **待开始** | 计划先用无需外部凭据的MCP Server构造小规模反事实环境 |
| 数据构建与模型 | **暂缓** | 等核心问题通过小规模验证后再决定是否投入 |

| 子文档或目录 | 作用 |
|---|---|
| [相关论文逐篇解读](相关论文逐篇解读.md) | 记录16篇论文的问题、方法、结果、局限和关系 |
| [研究资料与数据资源](研究资料与数据资源.md) | 记录论文、数据集、代码和插件生态入口 |
| `papers/` | 保存已下载论文PDF和校验文件 |
| `DeepSeek Harness与Cordis技术笔记.md` | 计划中的源码、定义和数学推导文档，当前尚未创建 |

## 一、一条主线

| 阶段 | 检索对象 | 回答的问题 | 主要风险 |
|---|---|---|---|
| **文档检索** | 事实、证据、参考文本 | 模型需要知道什么？ | 噪声与错误证据 |
| **Skill检索** | 工作流、步骤、脚本和资源 | 模型应该怎么做？ | 流程错误或能力组合不完整 |
| **Tool检索** | 已经安装并暴露的原子接口 | 模型下一步调用什么？ | 调错接口、参数或顺序 |
| **Plugin检索** | 可安装、可配置的运行时组件 | 当前Harness应该具备什么能力？ | 依赖冲突、越权、资源和状态污染 |

这不是严格的历史顺序，也不是后一层淘汰前一层。Plugin可以提供Tool和Skill，Tool负责执行原子操作，Skill负责描述程序性知识。变化在于：**Harness插件化以后，系统的最高组合单元从“上下文内容”上移到了“运行时组件”。**

## 二、我们已经走到哪里

我们此前在R3-Skill中研究了Skill Retrieval。文档通常可以分别判断相关性，但复杂任务需要多个Skill协作；每个Skill单独相关，不代表它们在当前需求下可以自然组合。因此，R3-Skill将问题拆成两部分：

- **单体相关性**：某个Skill是否与query相关；
- **查询条件兼容性**：多个Skill在当前query下是否应该共同出现。

我们保留了数据生成过程中原本会被丢弃的拒绝样本，用它训练和评测多Skill路由。这个工作仍然主要发生在**模型上下文层**：检索器决定向模型加载哪些程序性说明。

Tool Retrieval更接近执行层，研究如何从大量接口中选择工具、补齐依赖并规划顺序。现有工作已经覆盖平面检索、依赖图、任务分解、动态检索和MCP多工具编排，但多数工作有一个共同前提：**Tool所属的服务已经安装、连接并暴露给Agent。**

## 三、现有工作如何走到这里

| 研究阶段 | 代表工作 | 解决的问题 |
|---|---|---|
| 大规模工具数据 | **ToolBench** | 建立工具数据生成、检索、规划和执行流水线 |
| 专用工具检索 | **ToolRet** | 证明普通文档Retriever并不擅长工具检索 |
| 依赖补全 | **Graph RAG-Tool Fusion、TGR** | 补回query中没有直接出现的前置工具 |
| 组合与规划 | **GTool、DTDR、SkillWeaver** | 加入调用顺序、动态历史和任务分解 |
| 主动检索 | **ToolOmni** | 让Agent决定何时搜索、搜什么以及何时停止 |
| 状态与故障 | **ToolSandbox、C-World** | 引入持久状态、执行故障和动态约束 |
| MCP检索与编排 | **MCP-Atlas、MCP-Bench、MSC-Bench** | 评测候选工具使用、多Server编排和Server级检索 |
| 执行效果 | **DynamicMCPBench** | 从“工具名单正确”推进到“实际效果正确” |
| Harness影响 | **Harness-Bench** | 说明模型之外的Harness配置同样影响结果 |
| 组件生命周期 | **Cordis** | 定义运行时组件如何加载、依赖、替换和退出 |

目前最接近目标的拼图是：

| 所需能力 | 可借鉴工作 |
|---|---|
| 大规模召回 | ToolRet |
| Server级检索 | MSC-Bench |
| 复杂任务分解 | SkillWeaver |
| 环境变化 | C-World |
| 执行效果 | DynamicMCPBench |
| 生命周期 | Cordis |
| 系统级评价 | Harness-Bench |

但尚未看到一项工作完整覆盖：**从开放目录检索Plugin → 根据当前环境形成组件图 → 安装和激活 → 完成任务 → 安全卸载并检查状态恢复。**

具体方法、数据和局限见《相关论文逐篇解读》。

## 四、为什么继续研究Plugin

Plugin不是更长的Tool描述。一个插件可能同时：

- 注册多个Tools；
- 提供或依赖Services；
- 加载Skills和Prompt；
- 启动进程、连接和后台任务；
- 管理Memory、Credentials和Policy；
- 替换模型、沙箱或Agent Loop实现。

因此，Tool Retrieval回答的是“**现有能力中调用什么**”，Plugin Retrieval要回答的是“**哪些能力应该进入当前运行时**”。

| Tool Retrieval | Plugin Retrieval |
|---|---|
| 输入主要是query和tool描述 | 输入还包括网络、凭据、权限、平台、预算和当前Harness状态 |
| 输出通常是Top-K工具 | 输出更像插件配置增量和依赖图 |
| 假设工具已经可用 | 需要处理安装、Provider绑定、激活和退出 |
| 重点是调用正确 | 还要检查安全、成本、状态与生命周期 |

## 五、一个直观例子

用户要求：**检索论文、下载PDF、建立索引并生成报告。**

- **允许外网且有API密钥**：可以选择远程学术搜索和远程Embedding插件；
- **禁止外网但有本地论文镜像和GPU**：应切换到本地检索和本地模型；
- **文件系统只读**：下载和建立索引不可执行，应选择只读分析方案或明确拒绝；
- **已有向量库Provider**：应优先复用，而不是重复安装另一套存储。

同一个query在不同环境中可能对应不同插件方案。因此，结果不应只是固定Top-K，而应说明：**增加或移除哪些插件、依赖绑定到哪个Provider、采用什么配置和权限，以及任务完成后如何退出。**

## 六、目前需要验证什么

1. **环境是否真的改变答案？**如果去掉环境信息仍能得到相同方案，环境条件插件检索的必要性就很弱。
2. **是否真的需要学习式组合？**依赖补全、版本检查和Provider选择可能由规则或约束求解器完成，模型或许只需负责语义召回。
3. **真实插件是否存在可用契约？**公开生态能否稳定提供描述、配置、依赖、权限和生命周期信息，仍需统计。
4. **能否可靠评测？**除了召回率，还要检查配置可行性、任务完成、权限违规、资源泄漏和卸载后的状态恢复。

这些问题应当允许否定答案。早期实验的目的不是证明方案正确，而是判断问题是否真实、边界在哪里，以及学习方法相对规则系统是否有必要。

## 七、研究与业务线索

**研究流程：语义召回 → 组合验证 → 隔离执行 → 反馈学习。**

**业务价值：能力按需加载、减少默认工具暴露、复用已有Provider、控制冷启动与权限，并为插件安装和运行建立统一审计。**

下一步应先做小规模可执行验证：选择一批无需外部凭据的MCP服务器，对同一任务构造断网、无凭据、只读权限和Provider已存在等环境变化，比较query-only检索、确定性依赖规则和状态条件组合。只有当环境变化稳定地改变正确方案，且现有方法无法可靠处理时，再扩大数据和模型规模。
