# 相关论文逐篇解读

> 16篇已归档论文，不含我们自己的R3-Skill。每篇按问题、方法、数据与结果、贡献与局限、与插件检索的关系五项记录。

## 一、一句话看完16篇

| # | 论文 | 这篇证明了什么 |
|---|---|---|
| 1 | ToolBench | 建立了"抓API→生成指令→执行筛选→训练"的流水线，16,464个endpoint、126K条轨迹，后续工作大多沿用它的数据 |
| 2 | ToolRet | 通用Embedding不擅长工具检索：43K工具上NDCG@10只有约34，专门训练才能补上 |
| 3 | Graph RAG-Tool Fusion | 语义找入口、确定性依赖图补前置工具，是一条很强的规则基线，必须作为学习式方法的对照 |
| 4 | Tool Graph Retriever | 依赖图可以自动推断，但推断出的图仍不如人工图，且错误会沿图传播 |
| 5 | GTool | 图信息可以压成一个token喂给冻结LLM，但工具池扩到16K后增益明显缩小 |
| 6 | DTDR | 下一步工具由完整执行历史决定，检索需要每步重跑，不是任务开始时跑一次 |
| 7 | SkillWeaver | 分解粒度会卡住检索：修正分解把准确率从51.0%提到67.7%，但类别召回几乎没动 |
| 8 | ToolOmni | 检索和执行可以放进同一个策略一起训，但结构仍是"先检索完再执行"，失败退不回去 |
| 9 | ToolSandbox | 调对工具不等于任务完成，还要看状态、顺序和禁止操作；更大的模型有时反而更差 |
| 10 | C-World / ToolGym | 可以用真实MCP工具批量生成带故障和约束的环境，但正式评测只有50个场景 |
| 11 | MCP-Atlas | 真实容器化跨Server工作流下，最强模型通过率约62% |
| 12 | MCP-Bench | 工具名和Schema正确率超过98%，卡住的是高层规划、依赖处理和并行效率 |
| 13 | MSC-Bench | 功能等价的工具不该只认一个标准答案；扁平高召回常胜过先Server后Tool，但延迟更高 |
| 14 | DynamicMCPBench | 把评测从"工具名单"改成"实际效果"后，最强模型只有51.2% pass³，长链任务13% |
| 15 | Harness-Bench | 同模型换Harness配置最多差23.8分，能力必须按"模型+Harness配置"报告 |
| 16 | Cordis | 给出插件加载、依赖协调、替换、部分失败回滚和安全退出的形式语义与实现 |

按主题分组：

| 层次 | 论文 | 回答什么 |
|---|---|---|
| 大规模工具学习 | ToolBench、ToolRet | 数据从哪来，平面检索够不够 |
| 依赖与组合 | Graph RAG-Tool Fusion、Tool Graph Retriever、GTool、DTDR、SkillWeaver | 前置工具怎么补，顺序怎么定，任务怎么拆 |
| 主动检索与执行环境 | ToolOmni、ToolSandbox、C-World | 谁来控制搜索，状态和故障怎么算 |
| MCP评测 | MCP-Atlas、MCP-Bench、MSC-Bench、DynamicMCPBench | 多Server编排和等价工具怎么评 |
| Harness与运行时 | Harness-Bench、Cordis | 检索之外的系统因素有多大，装上之后怎么退 |

## 二、大规模工具数据与检索

### 1. [ToolBench：Towards Mastering Every Tool for LLMs](https://arxiv.org/abs/2307.16789)

**问题：** 如何自动构建足够大的真实API训练集，使开源模型能够检索、调用并组合未见过的工具。

**方法：** 从RapidAPI抓取并过滤API，由ChatGPT根据抽样API生成单工具和多工具指令，再通过真实API响应与DFSDT搜索有效轨迹，训练Tool Retriever和ToolLLaMA，并使用ToolEval评测。

**数据与结果：** 过滤后包含3,451个工具/服务、16,464个API endpoint和197,479条合格指令，执行筛选后得到126,486条指令—轨迹对。Retriever平均NDCG@1/@5约为78.0/84.9；DFSDT显著优于普通ReAct。

**贡献与局限：** 建立了“工具收集—任务生成—检索—执行—评测”的基础流水线，后续大量工作沿用其数据。任务由预选API反向生成，工具先验强；大量实验直接提供oracle API；Retriever只运行一次；RapidAPI和LLM Judge都会产生漂移。16,464是endpoint数量，不能直接等同于插件数量。

**与插件检索的关系：** 适合作为大规模语义召回和执行数据来源，但不覆盖插件安装、Server选择、权限、版本与生命周期。

### 2. [ToolRet：Retrieval Models Aren't Tool-Savvy](https://arxiv.org/abs/2503.01763)

**问题：** 传统IR模型在文档检索上表现很好，但能否从真实的大工具库中找到任务所需工具？

**方法：** 将34个工具使用数据源统一成标准IR格式，分别评估query-only和query+instruction，并从ToolACE、APIGen和ToolBench生成专用训练集。

**数据与结果：** 评测集包含43,215个去重工具和7,615个任务，训练集包含205,826条任务。Query-only设置下，最强通用Embedding的平均NDCG@10约为34，Completeness@10不到35%，说明通用Retriever并不天然擅长工具检索。专门训练后，检索和下游任务成功率均有提升。

**贡献与局限：** 目前规模最大、来源最杂的平面Tool Retrieval资源。它只做任务开始时的一次检索，不建模依赖、顺序和执行状态；不同数据源中功能等价的工具可能被漏标；工具粒度混合endpoint、函数和App。

**与插件检索的关系：** 适合作为开放插件目录第一阶段召回的训练和基线，但不能直接代表Plugin Package级检索。

## 三、依赖补全与工具组合

### 3. [Graph RAG-Tool Fusion](https://arxiv.org/abs/2502.07223)

**问题：** 语义检索容易找到最终工具，却容易漏掉登录、鉴权、参数生成等没有直接出现在query中的前置工具。

**方法：** 先用向量或混合检索找到Top-3入口工具，可选LLM重排，再在预定义Neo4j依赖图中通过DFS展开直接、间接和参数依赖。

**数据与结果：** ToolLinkOS包含573个虚构工具、1,569个query，平均每个工具6.3个依赖；另将ToolSandbox转成33节点依赖图。加入图展开后，ToolLinkOS和ToolSandbox上的mAP@10均显著高于Naïve RAG。

**贡献与局限：** 语义模型找入口、确定性图做依赖闭包，是一条必须对照的强基线。但提升有一部分来自预构造图与ground truth共享同一结构；ToolLinkOS工具不可执行；依赖图需要提前存在；入口工具漏召回时无法恢复。

**与插件检索的关系：** 如果Plugin Registry提供可信依赖图，这可能已经是足够强的规则方案，必须作为学习式Plugin Composer的对照。

### 4. [Tool Graph Retriever](https://arxiv.org/abs/2508.05152)

**问题：** 现实中通常没有完整依赖图，能否自动判断工具依赖并用它增强检索？

**方法：** 训练BERT三分类器判断工具A依赖B、无依赖或B依赖A，构造有向图，再通过无参数GCN将邻居依赖信息融入工具Embedding。

**数据与结果：** TDI300K实际包含约276K合成预训练pair和35,450人工微调pair，另有API-Bank人工测试集。API-Bank上，ToolBench-IR的Pass@10从0.624提升到0.788；人工依赖图仍优于自动图。

**贡献与局限：** 产出是依赖识别数据和可叠加到现有Retriever上的图增强模块。全量两两判别是O(N²)，分类错误会沿图传播；跨类别组合验证不足；Pass只表示真值工具被找全，不是执行成功。

**与插件检索的关系：** 适合推断隐式前置关系，但推断出的依赖必须经过契约或执行验证，不能直接当作运行时事实。

### 5. [GTool：Graph Enhanced Tool Planning](https://arxiv.org/abs/2508.12725)

**问题：** 除了找全工具，还要输出依赖和顺序正确的调用计划；历史轨迹形成的依赖图又可能不完整。

**方法：** 根据历史相邻调用建图，加入连接query和所有工具的超级节点，通过GNN将图压成一个连续graph token交给冻结LLM，并用Missing Dependency Prediction恢复被遮蔽的图边。

**数据与结果：** 主实验工具池只有15–40个；在这些数据上n-F1明显优于基线。扩到16K ToolBench API后仍有提升，但幅度缩小，说明大规模扩展并未完全解决。

**贡献与局限：** 把图信息转成LLM可消费的表示，从无序检索推进到有序计划。代价是依赖历史轨迹、需要训练GNN、难以零样本接入新插件，且没有真实执行和错误恢复。

**与插件检索的关系：** 更适合作为插件检索后的工作流规划器，而不是开放目录的第一阶段Retriever。

### 6. [Dynamic Tool Dependency Retrieval](https://aclanthology.org/2026.findings-acl.1680/)

**问题：** 下一步工具不仅由初始query决定，还由已经执行的完整历史决定；静态检索无法适应计划推进。

**方法：** 每一步用“用户query+当前部分计划”重新检索。DTDR-C按query聚类示范并建立高阶Markov图；DTDR-L用冻结MiniLM编码query和历史，只训练轻量线性分类器。

**数据与结果：** 使用TinyAgent和TaskBench数据，工具池仅17–41个。小模型上Function Selection Accuracy和成功率提升明显，同时减少Prompt中暴露的工具信息。

**贡献与局限：** 明确提出query-aware和full-history-aware的动态依赖检索。依赖高质量示范，工具规模小，分布外和长尾表现不稳；公开伪代码是否存在teacher forcing需要源码确认。

**与插件检索的关系：** 说明Plugin Retrieval可能在执行过程中反复发生，而不一定只在任务开始时运行一次。

### 7. [SkillWeaver：Compositional Skill Routing for LLM Agents](https://arxiv.org/abs/2606.18051)

**问题：** 复杂query通常包含多个原子子任务，因此需要先正确分解，再为每步检索Skill并形成DAG。

**方法：** 采用Decompose—Retrieve—Compose框架。SAD先进行一次粗分解和检索，再把召回的Skill词汇反馈给LLM重新分解，使任务粒度接近真实Skill库。

**数据与结果：** 使用2,209个MCP Server目录项和300个组合query。SAD将严格分解准确率从51.0%提高到67.7%，但CatR@1只从34.2%升至37.0%，且提升不显著，说明它主要修正分解粒度，而非解决最终排序。

**贡献与局限：** 把上游任务分解纳入Skill Routing，并证明分解粒度会限制检索。主要评类别召回而非精确Skill；Compose阶段缺少独立兼容性标注；30条端到端实验使用mock executor；所谓Skill更接近MCP Server目录项。

**与插件检索的关系：** 它是复杂任务插件组合的重要上游模块，但真实执行和环境条件仍然缺失。

## 四、主动检索、执行状态与环境

### 8. [ToolOmni](https://arxiv.org/abs/2604.13787)

**问题：** 静态Embedding Retriever和执行模型彼此割裂，无法让Agent主动决定何时搜索、搜什么和何时停止。

**方法：** 先用SFT冷启动，再通过解耦多目标GRPO联合训练检索与执行。Agent可多轮生成搜索query、筛选候选和决定停止，然后进入执行阶段。

**数据与结果：** 主要使用ToolBench的80K query，约28K检索轨迹和33K执行轨迹；检索最多4轮、执行最多6轮。端到端SoPR从ToolLLaMA-v2的43.33%提升到54.13%，主要增益来自RL。

**贡献与局限：** 主动检索和执行进了同一个策略，但运行结构仍是先检索完再执行；执行失败不能返回检索阶段；reward依赖gold tools，并高度绑定ToolBench。

**与插件检索的关系：** 提供从静态Top-K升级为Agentic Retrieval的方法参考，但底层向量Retriever仍然固定。

### 9. [ToolSandbox](https://arxiv.org/abs/2408.04682)

**问题：** 调用了正确工具不代表任务完成；评测还需要考虑对话澄清、持久状态、调用顺序和禁止操作。

**方法：** 通过Execution Context保存消息与数据库快照，由用户模拟器、Agent和执行环境交互。Milestone描述必须发生的状态变化，Minefield描述绝不能发生的行为，并用DAG约束顺序。

**数据与结果：** 包含1,032个场景、34个工具和11个领域。GPT-4o总分约73，但在信息不足场景只有42；更大的模型在状态依赖任务上有时反而更差，因为会错误并行调用本应串行的工具。

**贡献与局限：** 对中间状态和错误位置刻画得很细，适合做检索之后的执行评测。工具少、场景人工成本高、用户模拟器会出错，且不是开放目录检索。

**与插件检索的关系：** 可用于检查“插件找对了以后，状态和顺序是否正确”，不负责从大插件库发现候选。

### 10. [C-World / ToolGym](https://arxiv.org/abs/2601.06328)

**问题：** 如何构建可扩展、长时程、具有真实工具、复杂约束和故障状态的Agent训练与评测环境。

**方法：** 将环境定义为Action、Task、Transition、Reward。系统从MCP工具库采样可组合能力，生成任务，并由State Controller注入工具故障、状态漂移和动态约束；支持live MCP和World Engine模拟两种模式。

**数据与结果：** 整理并验证276个MCP Server、5,571个工具和204个应用，但主评测只有50个场景。World Engine上的模型排序与真实执行相关性较高，但这不代表每个响应和状态转移都与真实环境一致。

**贡献与局限：** 把大规模MCP工具、环境变化和执行反馈放进同一个环境生成框架。正式评测任务少，语义指标依赖LLM Judge，World Engine仍需人工类别卡，Retriever本身没有Recall/NDCG分析。

**与插件检索的关系：** 它可能是验证环境条件插件检索最有价值的执行环境，但需要重新构造Server级检索任务与Retriever基线。

## 五、MCP检索与编排评测

### 11. [MCP-Atlas](https://arxiv.org/abs/2602.00933)

**问题：** 现有MCP评测工具少、流程浅或过度依赖整体答案判断，无法覆盖真实跨Server工作流。

**设计：** 每题预挂载10–25个工具，其中3–7个目标工具和5–10个同类干扰项；任务通常需要3–6次调用，大多数跨Server，并包含条件分支。

**规模与结果：** 包含36个真实Server、220个工具和1,000个任务，其中公开500个。最强模型通过率约62%，主要失败来自没有正确使用工具或未完成全部子目标。

**贡献与局限：** 提供真实Server、容器环境和claim级部分分。但Server已预挂载、任务只读、主评分依赖LLM判断最终回答，不评插件安装与发现。

**与插件检索的关系：** 适合把“正确Server预挂载”这一条件去掉，改造成Server/Plugin Retrieval评测。

### 12. [MCP-Bench](https://arxiv.org/abs/2508.20453)

**问题：** 评测模糊用户需求下的工具识别、长链规划、跨Server协调、参数控制和结果Grounding。

**设计：** 根据工具输入输出发现依赖链，由LLM生成任务并改写为不含工具名的模糊指令；每题额外挂接10个干扰Server，支持单、双、三Server任务。

**规模与结果：** 包含28个Server、250个工具和104个任务。强模型工具名与Schema正确率普遍超过98%，但高层规划、依赖处理和并行效率仍然较弱。

**贡献与局限：** 有公开Server实现和较完整的多Server任务，适合作为最小可执行原型的起点。任务数少，生成和评分依赖LLM，Server仍预连接，执行结果不等于外部环境效果。

**与插件检索的关系：** 可将“已连接目标Server+干扰Server”改造成“从开放目录选择并挂载Server”的任务。

### 13. [ETOM / MSC-Bench](https://arxiv.org/abs/2510.19423)

**问题：** MCP具有Server→Tool层次，多个工具可能功能等价，固定唯一Gold Tool会误罚正确替代方案。

**方法：** 从Glama Registry收集Server，构造Equal Function Sets，并设计五级课程：直接工具识别、语义检索、单Server计划、跨Server计划和任务不可完成判断。

**数据与结果：** 包含491个Server、2,375个工具和2,075个任务。扁平高召回ToolShed通常优于先Server后Tool的MCP-Zero，但延迟更高；不存在适配所有模型的统一最佳路由方式。

**贡献与局限：** 目前最直接的Server/Tool层次检索Benchmark，且允许多种等价答案。但不真正执行工具，只评节点集合和计划，验证不了参数、效果和运行状态。

**与插件检索的关系：** 现有工作中最接近开放目录Plugin/Server Retrieval，可直接作为检索层基线和数据来源。

### 14. [DynamicMCPBench](https://arxiv.org/abs/2607.20531)

**问题：** Live和Stateful环境会让标准答案过期，固定Gold Tool List也会误罚不同但等效的执行路径。

**方法：** 先让Explorer在真实Server上完成任务，再将成功轨迹蒸馏成Effect Checkpoints、Minefields和必要偏序；候选Agent在确定性Replay环境中复现效果，不按最终回答文本评分。

**数据与结果：** 完整语料包含121个Live Server、2,051条成功参考轨迹和1,845个任务；论文实验使用750题。最强模型约51.2% pass³，31%的任务没有任何模型解决；长链任务聚合成功率仅13%。

**贡献与局限：** 把评测对象从工具名单推进到实际效果，并允许多条等效路径。但所需工具和干扰项仍直接暴露，不评独立Retriever；写任务依赖Sandbox；保守Scorer可能漏掉合法替代路线。

**与插件检索的关系：** 非常适合作为插件组合后的执行评测与数据生成方法，但需要在前面增加开放目录检索、安装和激活阶段。

## 六、Harness与组件生命周期

### 15. [Harness-Bench](https://arxiv.org/abs/2605.27922)

**问题：** Agent表现常被归因于Base Model，但Prompt、工具接口、状态、权限、重试和恢复等Harness配置同样决定结果。

**设计：** 固定任务、初始Sandbox、预算、超时和Evaluator，改变完整Harness配置，同时保留各Harness原生行为，记录工作区产物、轨迹和使用量。

**数据与结果：** 包含106个离线任务、6个可配置Harness、8个模型和5,194条轨迹。最佳与最差可配置Harness相差23.8分，说明Agent能力应按“Model+Harness Configuration”报告。

**贡献与局限：** 把Harness作为独立评测轴，同时看完成度、过程、安全和成本。比较的是整套Harness，归因不到单一机制；任务离线；过程评分依赖LLM Judge。

**与插件检索的关系：** 说明即使Retriever相同，不同工具注入、状态、权限和恢复策略也会改变最终效果，系统评测不能停在检索指标。

### 16. [Cordis：A Programming Paradigm for Spatiotemporal Composability](https://github.com/cordiverse/paper/blob/main/paper.pdf)

**问题：** 运行时组件动态加入、删除和替换时，如何完整撤销副作用，并在依赖出现、消失或更换时自动协调生命周期。

**方法：** Revertible Effect要求每个Context变换同时返回Inverse，由运行时按LIFO组合和回收；Reactive Coeffect要求组件声明依赖，由Context变化触发激活或停用。论文进一步定义Context、Component、Fiber、Committed View和Target View，并给出动态组合演算。

**证据：** 它不是Benchmark，没有任务和排行榜。形式结论依赖Inverse正确、Effect独立、依赖无环等假设；经验依据是Koishi四年积累的4,000多个社区插件，但Koishi使用的是Cordis v3，论文形式化的是v4。

**贡献与局限：** 为插件加载、依赖、替换、部分失败回滚和安全退出提供了形式语义和TypeScript实现。运行时不验证Inverse是否正确；系统边界外已经发送的消息、写入和支付无法严格撤销；没有受控性能或开发效率实验。

**与插件检索的关系：** Cordis不负责找插件，它解决的是检索完成后的装载、依赖协调、替换和卸载。

## 七、整体结论

这些工作已经分别覆盖了大规模召回、依赖图、任务分解、动态检索、真实状态、MCP编排、效果评分、Harness评测和组件生命周期，但尚未完整打通：

**从开放目录检索Plugin → 根据当前环境形成组件图 → 安装和激活 → 完成任务 → 安全卸载并检查状态恢复。**

对当前方向最有用的拼图是：ToolRet负责大规模召回，MSC-Bench负责Server级检索，SkillWeaver负责复杂任务分解，C-World负责环境变化，DynamicMCPBench负责执行效果，Harness-Bench负责系统级评价，Cordis负责生命周期语义。

## 八、从产品角度怎么使用这些工作

|产品问题|可参考工作|可借鉴内容|
|---|---|---|
|能力目录太大，模型不能全部看到|ToolRet、MSC-Bench|分层检索、候选集缩小、等价能力|
|复杂需求无法直接映射到单个能力|SkillWeaver、GTool、DTDR|任务分解、依赖、动态历史|
|检索正确但任务仍然失败|ToolSandbox、C-World、DynamicMCPBench|状态、故障、约束和效果评分|
|模型与Harness组合影响结果|Harness-Bench|按模型+Harness整体评价|
|能力加载后如何安全退出|Cordis|依赖顺序、回滚和生命周期|
|静态检索与执行脱节|ToolOmni|主动检索与执行反馈|

论文指标不能直接替代产品指标。Recall、NDCG和节点F1只能说明能力是否被找到；真实产品还要观察任务完成率、用户改写率、Tool失败、权限拦截、延迟、Cache Hit、冷启动和人工接管。后续选数据或方法时，应先说明它对应哪个产品问题，再判断是否值得接入。
